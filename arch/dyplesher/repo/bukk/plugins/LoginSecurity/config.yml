settings:
  password-required: false
  encryption: BCRYPT
  encoder: UTF-8
  PHP_VERSION: 4
  messager-api: true
  blindness: true
  fake-location: false
  session:
    use: true
    timeout (sec): 60
  timeout:
    use: true
    timeout (sec): 60
  table prefix: ls_
MySQL:
  use: false
  host: localhost
  port: 3306
  database: LoginSecurity
  username: root
  password: password
  prefix: ''
